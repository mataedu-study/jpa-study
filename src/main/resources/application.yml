spring:
  datasource:
    url: jdbc:mysql://localhost:3308/jpa?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # dialect: org.hibernate.dialect.Oracle0gDialect # 방언 설정 가능
        format_sql: true
        #show_sql: true

logging.level:
  org.hibernate.SQL: debug
#    org.hibernate.type: trace